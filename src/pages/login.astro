---
import LoginForm from "../components/LoginForm";
import AppLayout from "../layouts/AppLayout.astro";

const _LoginForm = LoginForm;
const _AppLayout = AppLayout;
---

<AppLayout title="登录 - GoldWatch">
  <LoginForm client:only="react" />
</AppLayout>
